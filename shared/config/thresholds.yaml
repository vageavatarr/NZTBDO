version: 1
perception:
  detection_confidence_min: 0.55
  engage_confidence_min: 0.65
  near_radius_m: 8.0
  front_cone_angle_deg: 90
  front_cone_range_m: 7.0
  runtime:
    detector_backend: auto
    model_path: data/models/enemy_detector.pt
    confidence_min: 0.45
    pixel_to_meter: 0.01
    max_targets: 8
    enemy_class_ids: []

combat:
  clear_timeout_sec: 2.5
  progress_timeout_sec: 12.0
  action_rate_limit_hz: 8
  post_skill_pause_sec: 1.5
  post_move_skill_block_sec: 0.5

navigation:
  stuck_timeout_sec: 6.0
  waypoint_reacquire_max_m: 20.0

input_control:
  dry_run: true
  require_foreground_window: true
  bind_to_process: false
  allow_background_input: false
  allowed_window_titles: []
  allowed_process_names: []
